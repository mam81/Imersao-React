import styled from 'styled-components';

import logo from '../../assets/logo.png'

const LogoDev = styled.img.attrs({ src: logo, alt: 'Logo da DevFlix' })`
    height: 40px;
`;

export default LogoDev;