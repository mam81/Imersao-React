import styled from 'styled-components';

const LinkDev = styled.a`
    text-decoration: none;
    font-weight: bold;
    color: var(--color-secondary-medium);
    cursor: pointer;
`;

export default LinkDev;